server:
  port: 8080
  tomcat:
    uri-encoding: utf-8

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/cloud_service
    username: root
    password: root
  profiles:
    active: dev
  thymeleaf:
    cache: false

